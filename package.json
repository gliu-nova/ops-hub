{
  "name": "ops-hub",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=ops-hub",
    "db:local": "wrangler d1 execute ops-hub --local --file=./migrations/0001_heartbeats.sql",
    "db:remote": "wrangler d1 execute ops-hub --remote --file=./migrations/0001_heartbeats.sql"
  },
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260624.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.104.0"
  }
}